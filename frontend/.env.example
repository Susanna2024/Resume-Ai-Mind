# Optional. Only needed if the frontend is deployed separately from the
# backend (e.g. frontend on Vercel, backend on Render). Leave unset for local
# dev (uses Vite's proxy) or for a combined single-service deployment.
# VITE_API_URL=https://your-backend.onrender.com
